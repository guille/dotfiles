#!/usr/bin/env bash
# Execute with parameter 'arch' or 'ubuntu'

if [ -z "$1" ]; then 
	echo usage: $0 arch\|ubuntu
  exit 1
fi

command_exists () {
    type "$1" &> /dev/null ;
}

# Select the right install command and update system
# if [ "$1" = "arch" ]; then
# 	INSTALL="sudo pacman -S"
# 	sudo pacman -Syu
# elif [ "$1" = "ubuntu" ]; then
# 	INSTALL="sudo apt-get -y install"
# 	PPA_INSTALL="sudo add-apt-repository"
# else
# 	echo usage: $0 arch\|ubuntu
# 	exit 2
# fi

# Install zsh and set it as default shell
# $INSTALL zsh
# chsh -s /bin/zsh

# if [ "$1" = "ubuntu" ]; then
# 	echo "installing ppas from sources/ppas"
# 	cat sources/ppas | xargs $PPA_INSTALL
# 	erlang ppa
# 	wget https://packages.erlang-solutions.com/erlang-solutions_1.0_all.deb && sudo dpkg -i erlang-solutions_1.0_all.deb
# 	echo "updating system"
# 	sudo apt-get update -y && sudo apt-get upgrade -y && sudo apt-get dist-upgrade -y
# fi

# echo "installing programs from source/apps"
# cat sources/apps | xargs $INSTALL
# transmission, libreoffice is preinstalled in ubuntu
# ubuntu: pip install livestreamer

# echo "installing rvm"
# curl -sSL https://get.rvm.io | bash -s stable
# install ruby version
# rvm install 2.3-head
# Installing gems
# GEM_INSTALL="gem install"
# cat sources/gems | xargs $GEM_INSTALL


# Sublime package control
# "BracketHighlighter",
# "Elixir",
# "Package Control",
# "SublimeLinter",
# "SublimeLinter-contrib-elixirc",
# "SublimeLinter-pylint",
# "SublimeLinter-ruby"

# Does the symlinks
# . links

# if [ "$1" = "arch" ]; then
# 	Enable and start NetworkManager
# 	feh --bg-scale /path/image.jpg
# fi

# Print to-do list
# echo "Creating a to-do file with more possible configurations"
# if [ "$1" = "ubuntu" ]; then
# 	echo "* Install chrome" >> todo
# 	echo "* Set-up tv renamer in nautilus-actions" >> todo
# 	echo "* Change gtk theme and icon theme to Numix in unity-tweak-tool" >> todo 
# 	echo "* Change gnome-terminal to use Inconsolata font and Solarized Dark theme" >> todo
# else
# 	echo "* Change xfce4-terminal to use Inconsolata and set the opacity" >> todo
# fi
# echo "* Set up git config --global user.name and user.email"

# USAGE
# cd
# mkdir projects
# cd projects
# git clone https://github.com/guille/dotfiles.git
# run install script